{
  // Define o nome do pacote 
  "name": "anabolic",

  // Define a versão do pacote como "1.0.0"
  "version": "1.0.0",

  // Descreve brevemente a finalidade do aplicativo
  "description": "App em electron para mostrar uma playlist anabolica de rock/metal",

  // Especifica o arquivo principal do aplicativo como "index.js". Isso é o que será executado quando o aplicativo for iniciado.
  "main": "index.js",

  // Define um script chamado "start", que executa o arquivo "index.js" usando o Electron.
  "scripts": {
    "start": "electron index.js"
  },

  // Define as configurações para a construção do aplicativo.
  "build": {

    // Define o ID do aplicativo como "anabolic.app".
    "appId": "anabolic.app",

    // Define o nome do produto como "Anabolic Playlist".
    "productName": "Anaboli Playlist",

    // Especifica o diretório de saída para os arquivos de construção como "dist".
    "directories": {
      "output": "dist"
    },

    // Lista os arquivos que devem ser incluídos no pacote de construção.
    "files": [
      "index.js",
      "index.html",
      "index.css"
    ],

    // Configurações específicas para a construção no macOS.
    "mac": {
      // Define o formato de distribuição do arquivo de construção como um arquivo de imagem de disco DMG.
      "target": "dmg",
      
      // Especifica o ícone do aplicativo para macOS localizado em "build/nomeDoArquivo.extensão, no mininmo 256x256"
      "icon": "build/nomeDoArquivo.extensão"
    },

    // Configurações específicas para a construção no Windows.
    "win": {
      // Define o instalador do NSIS como o formato de distribuição do arquivo de construção.
      "target": "nsis",

      // Especifica o ícone do aplicativo para Windows localizado em "build/nomeDoArquivo.extensão", no mininmo 256x256.
      "icon": "build/nomeDoArquivo.extensão"
    }
  }
}
